export default function Welcome({ setStart }) {
    return(
        <>
            <>
                <img src="" />
                <h1>ZapRecall</h1>            
            </>
            <button onClick={() => setStart(true)}>Começar Recall</button>
        </>
    )
}